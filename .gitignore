# Secrets / live credentials
access_token.txt
totp_config.json
totp_qrcode.png

# Runtime state (production data, not code)
day_state.json
open_positions.json
user_config.json
bot_status.json
trade_history.jsonl

# Databases (research.db, production.db — created in later sessions)
*.db
*.db-wal
*.db-shm

# Python hygiene
venv/
__pycache__/
*.pyc
.DS_Store
instruments_cache*.json
signal_logs/

# Runtime-generated files
bot.log
watchlist_daily_range.json
pending_orders.json
pending_orders.json.lock

# fno_bot runtime state (production data, not code) -- distinct filenames
# from the equity bot's own by design (isolation), so needs its own entries
fno_bot/audit_logs/
fno_bot/shadow_logs/
fno_bot/instruments_cache*.json
fno_open_positions.json
fno_open_positions.json.lock
fno_trade_history.jsonl
fno_bot_status.json
fno_option_buying_v1_trades.jsonl
fno_option_buying_v1_state.json
fno_option_buying_v1_status.json
shared_capital_ledger.json
shared_capital_ledger.json.lock
fno_bot/user_config_fno.json
